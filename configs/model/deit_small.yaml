name: "deit_small_patch16_224"
pretrained: true
num_classes: 1000
input_size: 224
base_lr: 0.0001
weight_decay: 0.05
epochs: 30

dataset:
  name: imagenet
  data_dir: /path/to/imagenet    # ← 실제 경로로 수정
  num_workers: 8